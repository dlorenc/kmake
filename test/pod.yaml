apiVersion: v1
kind: Pod
metadata:
  name: backend
  labels:
    name: backend
spec:
  containers:
  - name: backend
    image: gcr.io/dlorenc-vmtest2/frontend:replaceme
    ports:
    - containerPort: 80
